# Environment Variables for SpeakSmart Client

# Backend API URL
VITE_BACKEND_URL=http://localhost:8000

# Google OAuth Configuration
# Get these from Google Cloud Console: https://console.cloud.google.com/
# 1. Create a new project or select existing
# 2. Enable Google+ API
# 3. Go to Credentials > Create Credentials > OAuth 2.0 Client IDs
# 4. Set Authorized redirect URIs to: http://localhost:5173/auth/callback (for dev)
VITE_GOOGLE_CLIENT_ID=your_google_client_id_here

# GitHub OAuth Configuration  
# Get these from GitHub Developer Settings: https://github.com/settings/developers
# 1. Go to OAuth Apps > New OAuth App
# 2. Set Authorization callback URL to: http://localhost:5173/auth/callback (for dev)
VITE_GITHUB_CLIENT_ID=your_github_client_id_here

# Note: For production, make sure to update the redirect URIs to your production domain
# Production redirect URI should be: https://yourdomain.com/auth/callback